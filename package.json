{
  "name": "agentpay-full-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fully functioning AgentPay website with static frontend, plain Node.js backend, listing dashboard, disputes, wallet ledger, messaging, escrow primitives, and payment provider architecture.",
  "scripts": {
    "backend:start": "npm --prefix backend-service start",
    "frontend:start": "node preview-server.mjs",
    "test": "npm --prefix backend-service test"
  },
  "engines": {
    "node": ">=20"
  }
}
